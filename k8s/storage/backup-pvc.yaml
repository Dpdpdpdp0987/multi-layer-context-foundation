apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-storage
  namespace: context-foundation
  labels:
    app: backup
    tier: data
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: archive
  resources:
    requests:
      storage: 500Gi
